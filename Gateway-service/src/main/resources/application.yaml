spring:
  application:
    name: gateway-service

  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8070

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: food123456789app123456789app123456789