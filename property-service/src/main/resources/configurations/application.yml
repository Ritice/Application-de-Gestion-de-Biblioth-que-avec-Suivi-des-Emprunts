spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bibliotheque_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ritice
    password: ritice
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration