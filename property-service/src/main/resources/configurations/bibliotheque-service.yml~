spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    table: flyway_history_bibliotheque

threads:
  virtual:
    enabled: true

server:
  port: 8082